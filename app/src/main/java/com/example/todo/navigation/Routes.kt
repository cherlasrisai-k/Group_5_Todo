package com.example.todo.navigation

enum class Routes(val route: String) {
    LOGIN("login"),
    REGISTER("register"),
    MAIN("main"),
    HOME("home"),
    ACTIVE("tasks"),
    HISTORY("history")
}
